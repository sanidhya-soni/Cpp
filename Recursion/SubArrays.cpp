#include<iostream>
#include<vector>
#include<limits.h>
using namespace std;

void subarrays(vector<int> arr, int x) {
    
}

int main() {

    int n;

    cout << "Enter length of Array: ";
    cin >> n;

    vector<int> arr(n, 0);

    for(int i = 0; i < arr.size(); i++) {
        cout << arr[i] << endl;
    }

    return 0;
}
